coverage:
  status:
    patch: false
    project:
      default:
        threshold: 1 # Allow coverage to drop by up to 1% in a PR before marking it failed
  range: 30..60

ignore:
  - "test"
  - "third_party"
  - "source/util.cpp"
  - "include/LuaBridge"

comment:
  require_changes: true
